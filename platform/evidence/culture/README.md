<!-- Purpose: holds the Culture & Growth agent's generated weekly evidence rollups — counts only, never hand-edited. -->

# Evidence rollups — `culture` scope

Generated by this scope's weekly evidence-rollup cron (cron id TODO(gate) — set when the scope is compiled into `platform/deploy-layer/otpless/crons.md`) per `packs/shared/metrics/SKILL.md`. One immutable file per closed window, named `<window_end>.md`, following the schema in `../README.md`.

Empty until the deployment is live and the agent has produced drafts — there is no evidence yet because the system has not run. An empty directory is the honest state; do not seed it with an example rollup, because a plausible-looking file here could be mistaken for real evidence in a promotion PR.

This pack's promotable action-classes are unusually narrow (`packs/culture/config/evidence.md`) — most of what the agent does is scheduling/tracking, not content generation — so expect this directory to accumulate slowly even once live.

Counts only. Nothing in this directory may identify a person (ADR-008) — the eval harness enforces that on every file added here.
