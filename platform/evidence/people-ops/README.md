<!-- Purpose: holds the People-Ops agent's generated weekly evidence rollups — counts only, never hand-edited. -->

# Evidence rollups — `people-ops` scope

Generated by the `people-ops-evidence-rollup` cron (21:00 IST Sunday) per `packs/shared/metrics/SKILL.md`. One immutable file per closed window, named `<window_end>.md`, following the schema in `../README.md`.

Empty until the deployment is live and the agent has produced drafts. Do not seed it with an example rollup — a plausible-looking file here could be mistaken for real evidence in a promotion PR.

This scope runs Strict posture on HRMS writes (`platform/deploy-layer/otpless/command-policy.md` §1/§7), so its action-classes carry that precondition into any promotion in addition to the usual gate.

Counts only. Nothing in this directory may identify a person (ADR-008) — enforced by the eval harness on every file added here.
