<!-- Purpose: the living ledger of every human-gated decision blocking or governing the platform — the single answer to "what is the founder/CTO blocking?" -->

# Human Gate Ledger

This is the live status of every item that cannot be closed by an agent — not because of a skill gap, but because the org's guardrails require a specific human to decide, credential, or authorize it. It is sourced from `docs/RUNBOOK_DEPLOY.md` §1, plus two standing gates that recur throughout the build (agent naming, trust-ladder promotions).

## Rules

1. **Agents never stall on a gate.** When work hits a gate, the agent records it here, stages the exact next step (command, config, or PR), and moves to the next item. A blocked step costs the operator one paste or one approval — never a redesign.
2. **Every gate names exactly one accountable human** (Founder or CTO per the runbook's assignment). Ambiguity here just means asking that person, not defaulting to "someone."
3. **A gate is closed only by that human** — an agent cannot self-approve a credential, an OAuth grant, a name, or a trust promotion, even if it has the technical means to fake it.
4. **This table is maintained continuously**, not just at deploy time — update status the moment a gate opens, is worked around differently, or closes.

## Gate table

| ID | Gate | Owner | Why it is human | What unblocks when it lands | Current workaround in the build | Status |
|---|---|---|---|---|---|---|
| G1 | Cloud account: Fly.io org (or AWS) | CTO | credential / spend | `bootstrap-qm.sh` can create the deployment repo and run `qm init --target fly` | Preflight in `bootstrap-qm.sh` checks for `FLY_ORG` / `FLY_API_TOKEN` (or AWS equivalent) and reports MISSING; nothing provisioned | OPEN |
| G2 | Managed Postgres — **fly target: not a gate.** `qm up` provisions Managed Postgres and private object storage itself; `DATABASE_URL` is required only for the `aws` target (verified against qm's own `.env.example`) | CTO | credential / spend | qm's persistence layer can be wired during `qm init` | Preflight checks for a `DATABASE_URL`-shaped env var; deploy-qm skill will provision/attach once G1 is closed | N/A on fly (preflight reports it as such); OPEN only if the target ever changes to aws |
| G3 | Anthropic API key + monthly budget cap | Founder | credential / spend | Platform model access for every scope | Preflight checks for `ANTHROPIC_API_KEY`; budget cap and 50/80% alert thresholds are config the agent can propose but the cap value itself is a spend decision | OPEN |
| G4 | GitHub org repo `qm-deploy` created | CTO | credential (org repo-create rights) | `bootstrap-qm.sh` step "create deployment repo" can run | Preflight checks `gh auth status` succeeds against the target org; script refuses to run `gh repo create` until this passes | OPEN |
| G5 | Google Workspace user `recruiting@otpless.com` + OAuth client (Gmail/Calendar), SPF/DKIM verified | CTO | OAuth / DNS-DKIM | Recruiter agent can draft (L0) and later send (L1) real email | Preflight checks for OAuth client env vars; all current email work runs against synthetic fixture threads only, never this mailbox | OPEN |
| G6 | Slack app (bot) created, added to #hiring and #people | CTO | OAuth / credential | @agent can respond in Slack, standup cron can post | Preflight checks for `SLACK_BOT_TOKEN`; token is never written to git, only read from env at deploy time | OPEN |
| G7 | Notion internal integration token, shared with Careers page and Applicants DB | Founder | credential / OAuth | Notion read/write round-trip (real Applicants DB) | Preflight checks for `NOTION_TOKEN`; verification checklist uses a synthetic applicant row in a scratch/test location, never the shared Applicants DB, until this lands | OPEN |
| G8 | Agent public-name decision | Founder | naming (explicitly reserved to founder per CLAUDE.md) | Mailbox display name, Slack handle, AI-disclosure line can be finalized | Preflight passes on EITHER the `AGENT_PUBLIC_NAME` env var OR a filled-in "Agent public name" row (no `TODO(gate)`) in `platform/deploy-layer/otpless/org-config.md`, whichever lands first; internal/code references use the neutral scope name `recruiter` until then | OPEN |
| G9 | Web sign-in broker: admin email, verified sender, Resend API key or SMTP creds (or external IdP registration) | CTO | credential / DNS | qm's one-time sign-in link flow can be enabled | Preflight checks for `RESEND_API_KEY` (or SMTP vars) or, if external IdP, for the callback-URL registration; web UI check in verification checklist stays unchecked until then | OPEN |
| G10 | Provider decision: Fly.io, binding per deployment directory | CTO | spend / infra decision | — | **DECIDED 2026-08-01**: Fly.io, slug `otpless` (local, not globally unique). `bootstrap-qm.sh` targets `--target fly` unconditionally; org + billing (G1) still required to execute | DECIDED — G1 still open |
| G11 | Trust-ladder L1 promotion (recruiter agent, first action class) | Founder | trust-ladder promotion (ADR-004) | Recruiter can auto-send routine sends for that action class | Agent stays at L0 (drafts-only); promotion PR will be opened once ≥95% unedited-draft-acceptance evidence exists over the ADR-004 window, but the merge itself is a human act | NOT YET APPLICABLE (pre-deploy) |
| G12 | MCP server package verification (packages named in `.mcp.json` are unverified until the deployment is wired; Slack server `@modelcontextprotocol/server-slack` is npm-flagged deprecated) | CTO | deploy-time verification / vendor risk, not a data gate | Connectors go live only against confirmed, supported packages | `platform/contracts/slack.md` and `platform/contracts/README.md` flag the deprecated package; no MCP server is wired against production credentials until this closes | OPEN |
| G13 | Gmail MCP send capability (extend beyond draft/label/read so an approved send can execute) | CTO | MCP extension is a build/spend decision at deploy time, not a draft-skill workaround (ADR-007) | Recruiter's candidate-email action-classes can be considered for L1 auto-send | Every skill's output contract ends at a created draft (ADR-007); `platform/contracts/gmail.md` "Capability gaps today" tracks this; no raw-HTTP or headless-browser workaround is used in the meantime | OPEN |
| G14 | HRMS provider decision: Keka vs RazorpayX Payroll (master PRD §11 open question 1) | Founder | vendor/spend decision affecting payroll and comp administration | `platform/contracts/hrms.md` can be filled with real field/endpoint shape; blocks G15 and any HRMS MCP work | `platform/contracts/hrms.md` is written against the capability (leave balance, attendance, employee record, payroll cutoff calendar), not a vendor; every HRMS-dependent skill runs against synthetic fixtures only | OPEN |
| G15 | HRMS API credentials (OAuth/API key) for the People-Ops agent's own machine identity, scoped read-only where the vendor's permission model supports it | CTO | credential / OAuth, tied to whichever vendor account G14 selects | People-Ops's HRMS read-only integration (leave/attendance/employee-record queries) goes live against real data | No HRMS MCP server is wired; `platform/contracts/hrms.md` "Capability gaps today" tracks this; preflight would check for the vendor's credential env var once G14 lands, same pattern as `bootstrap-qm.sh`'s other credential checks | OPEN — blocked on G14 |
| G16 | Notion Employees DB creation (person, role, manager, start date, comp, ESOP, documents status, lifecycle stage — master PRD §6) + machine-user grant for Onboarder and People-Ops | Founder | Notion workspace object creation + schema decision (Notion workspace admin) | Onboarder can write real lifecycle-stage/documents-status rows; People-Ops can cross-reference real employee records instead of fixtures | `platform/contracts/notion-employees.md` states the field shape only, no data-source ID; onboarding/people-ops skills run against a scratch/test location, never a real Employees DB, until this lands (same convention as G7's Applicants DB scratch-location workaround) | OPEN |
| G17 | Notion Policies wiki creation (the sole citable source for People-Ops policy Q&A — master PRD §4, §6) + machine-user grant for People-Ops | Founder | Notion workspace object creation; initial policy content is authored by the Founder/People Lead — policy changes are never delegated (`command-policy.md` §4) | People-Ops's policy-Q&A skill can fetch and cite real pages instead of refusing to answer | No Policies wiki exists; the `policy-qa` skill (`packs/people-ops/policy-qa/SKILL.md`) must refuse to answer any policy question until real, citable pages exist — never invented, and never `brain/playbooks/` prose cited as if it were the approved wiki page | OPEN |
| G18 | BGV vendor account: SpringVerify or OnGrid — which one, plus contract/account setup (master PRD §4; `packs/onboarding/config/vendors.md`) | Founder | vendor selection + spend + compliance decision (employee background verification) | Onboarder's BGV skill can initiate/track real background-verification cases instead of only drafting requests | Per `packs/onboarding/config/vendors.md`, no skill may call a vendor API; BGV initiation is always a drafted request handed to a human to submit through the vendor's own portal | OPEN |
| G19 | Compile per-action-class rows into `platform/deploy-layer/otpless/command-policy.md` from the scope files, at deployment time | CTO | trust-ladder enforcement (ADR-004) — the policy file is what actually denies an action, so a class with no row cannot be promoted or enforced by name | Any trust-ladder promotion becomes citable and enforceable; until then every class sits at the L0 floor by default | Scope files (`platform/deploy-layer/otpless/scopes/*.md`) carry the authoritative action-class tables and the `evidence.md` files carry the slugs; nothing is promotable until they are compiled in, which is correct because no evidence exists yet | NOT YET APPLICABLE (pre-deploy) |
| G20 | WhatsApp Business API account + sender number (WABA ID, phone-number ID) | CTO | credential / platform provisioning, same category as the Slack app (G6) and Google Workspace user (G5) — CTO administers the underlying platform accounts | Recruiter/Onboarder/future-scope skills can draft against a real sender identity instead of a synthetic fixture; still draft-only until send capability is separately verified (ADR-007) | `platform/contracts/whatsapp.md` states the capability shape only, no real WABA ID or number; every skill that would use this channel outputs message text for manual send until this lands | OPEN |
| G21 | WhatsApp template-message approval + compliance review for HR/recruiting use | Founder | compliance/content decision — WhatsApp Business requires pre-approved templates for business-initiated messages, and clearing recruiting/HR content for that use is a content and compliance call, not a credential | No message may go out on any template, at any trust level, until this closes | `platform/contracts/whatsapp.md` "Capability gaps today" tracks this as a hard gate independent of G20; no skill assumes an approved template exists | OPEN |
| G22 | Decision: OTPLESS's own in-house WhatsApp infrastructure vs a separate provider/account for HR/recruiting outreach | Founder | vendor/compliance/separation decision — reusing product infrastructure for HR outreach has its own compliance and brand-separation questions and is not an automatic yes just because the capability exists in-house | Which sender identity G20 actually provisions against, and whether a distinct WABA is needed for HR traffic | `platform/contracts/whatsapp.md` "Capability gaps today" flags this as gated separately from the MCP-build decision; no skill or config assumes the product's own WhatsApp number is reused | OPEN |
| G23 | Comp-band market-data source (external salary-benchmark data) for the Analyst's comp-band-drift-vs-market deep-dive | Founder | vendor/spend decision, same category as the HRMS provider decision (G14) | `platform/deploy-layer/otpless/scopes/analyst.md` and a future contract file can be filled with a real source instead of a capability-only placeholder | `scopes/analyst.md` states the need against the capability, no vendor named; the monthly deep-dive cron reports that section "unreachable" until this lands | OPEN |
| G24 | Pulse-survey tooling decision (which tool/vendor runs pulse surveys) | Founder | vendor/spend decision, same category as the HRMS provider decision (G14) and the BGV vendor decision (G18) | `platform/deploy-layer/otpless/scopes/culture.md` and a future contract file can be filled with a real tool instead of a capability-only placeholder; unblocks the `culture-pulse-survey-cycle` cron's real cadence | `scopes/culture.md` states the need against the capability, no tool named; the pulse-survey cron reports "not yet connected" until this lands | OPEN |
| G25 | Culture tracker Notion object: create the database the `culture` scope writes its checklist/cadence records to, plus a machine-user grant | Founder | credential / Notion workspace admin — same pattern as G16/G17 | The Culture & Growth agent can track review cycles, offboarding checklists, and 1:1 cadence; a `platform/contracts/` file for the object can be written against a real schema | `packs/culture/config/notion.md` carries the pack's read of the shape it will need; no contract file exists yet and no ID is invented. The agent has no write target until this lands | OPEN |
| G26 | Node.js runtime >= 24 on every machine that runs qm CLI commands (`bootstrap-qm.sh`, the `deploy-qm` skill, any operator machine) | CTO | environment provisioning — qm hard-refuses to run below Node 24; not a credential, but still requires CTO-owned dev/ops machine setup | `bootstrap-qm.sh` preflight can pass and `qm init`/`setup`/`check`/`plan`/`up` can run at all | Preflight in `bootstrap-qm.sh` checks `node -v` and reports the detected version against the >= 24 requirement (e.g. build sandbox currently reports `v22.22.2`, MISSING); nothing provisioned automatically | OPEN |
| G27 | Sprites egress proxy: set `SPRITES_EGRESS_PROXY_URL` on core so agent sandbox traffic is forced through it | CTO | security posture — without it qm logs `sandboxes run with NO egress enforcement (fail-open)`, meaning nothing structurally stops a sandbox reaching an arbitrary host | Sandbox egress becomes enforceable rather than advisory, which is a precondition for any real candidate PII entering the system (DPDP, master PRD §6) | Observed live on the first deployment. Acceptable only while the sandbox runs a demo skill with no connectors wired; **must close before the first real candidate record** | OPEN |
| G28 | Resend sending-domain verification for `otpless.com` (DNS records) | Founder | DNS / domain ownership | Sign-in links and any candidate-facing mail send from `no-reply@otpless.com` instead of Resend's test sender, which only delivers to the account owner's own address | Deployment runs on `onboarding@resend.dev`; sign-in works for the Resend account owner only, so the CTO cannot yet be added to `AUTH_ALLOWED_EMAILS` usefully | OPEN |

Note on G10: this is the one row where the human decision itself is already closed (Fly.io, per RUNBOOK §1 item 10 and ADR-001 staging note) — what remains open is the credential/account underneath it (G1), not the choice.

## How to close a gate

**What the human does** (per gate):
- G1–G7, G9: obtain/create the credential or account, then set the corresponding environment variable(s) in the deployment environment (never commit them to git). `bootstrap-qm.sh --dry-run` (the default) lists the exact env var name(s) it expects for each gate.
- G8: the founder states the agent's public name — either export `AGENT_PUBLIC_NAME` in the deployment environment, or paste the name into the "Agent public name" row of `platform/deploy-layer/otpless/org-config.md` (replacing the `TODO(gate)` placeholder), or hand either to the agent to do so. Either source alone satisfies preflight; org-config.md is the durable record either way.
- G11: the founder reviews the promotion PR the agent opens once evidence qualifies, and merges it (or doesn't).

**What the agent runs afterward**:
- After any of G1–G7, G9 land: re-run `platform/scripts/bootstrap-qm.sh` (no flags) to re-check preflight. Once all required vars for a given step show PASS, run it again with `--apply` to execute that stage.
- After G8 lands (either source): re-run `platform/scripts/bootstrap-qm.sh` (no flags) — preflight reports which source (env var or org-config.md) satisfied it. If the env var was the source, still update `platform/deploy-layer/otpless/org-config.md`'s "Agent public name" row so it stays the durable record; propagate to Slack handle / mailbox display name / disclosure line, and re-verify per `platform/scripts/verify-deployment.md`.
- After G11 lands (PR merged): update the command policy (`platform/deploy-layer/otpless/command-policy.md`) to reflect the new autonomy level for that action class, and note the change in `brain/` per ADR-003 in the same commit.

## Status legend

- **OPEN** — blocking, no credential/decision provided yet.
- **DECIDED** — the human decision is made; a dependent credential/account may still be open (see G10).
- **NOT YET APPLICABLE** — gate exists but its preconditions (e.g. deploy live, evidence window elapsed) haven't been reached.
- **CLOSED** — human has provided the input; agent has verified and moved the corresponding runbook step from staged to executed.
